(()=>{const header=document.querySelector('[data-header]'),menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.site-nav');const update=()=>header?.classList.toggle('scrolled',scrollY>30);update();addEventListener('scroll',update,{passive:true});menu?.addEventListener('click',()=>{const open=document.body.classList.toggle('menu-open');menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'Fermer le menu':'Ouvrir le menu')});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{document.body.classList.remove('menu-open');menu?.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.05,rootMargin:'0px 0px 80px'});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const items=[...document.querySelectorAll('.gallery-item')],filters=document.querySelectorAll('.filter'),counter=document.querySelector('[data-visible-count]');filters.forEach(filter=>filter.addEventListener('click',()=>{filters.forEach(b=>{b.classList.remove('active');b.setAttribute('aria-pressed','false')});filter.classList.add('active');filter.setAttribute('aria-pressed','true');const cat=filter.dataset.filter;let count=0;items.forEach(item=>{const visible=cat==='all'||item.dataset.category===cat;item.hidden=!visible;if(visible){count++;item.animate([{opacity:0,transform:'translateY(14px)'},{opacity:1,transform:'none'}],{duration:420,easing:'ease-out'})}});if(counter)counter.textContent=count+' photographie'+(count>1?'s':'')}));const box=document.querySelector('.lightbox');if(box&&items.length){const image=box.querySelector('img'),title=box.querySelector('figcaption b'),meta=box.querySelector('figcaption span');let current=0;const visible=()=>items.filter(i=>!i.hidden);const show=index=>{const list=visible();current=(index+list.length)%list.length;const item=list[current],source=item.querySelector('img');image.src=source.src;image.alt=source.alt;title.textContent=item.dataset.title;meta.textContent=item.dataset.meta};items.forEach(item=>item.addEventListener('click',()=>{current=visible().indexOf(item);show(current);box.showModal()}));box.querySelector('.prev').addEventListener('click',()=>show(current-1));box.querySelector('.next').addEventListener('click',()=>show(current+1));box.querySelector('.lightbox-close').addEventListener('click',()=>box.close());box.addEventListener('click',e=>{if(e.target===box)box.close()});addEventListener('keydown',e=>{if(!box.open)return;if(e.key==='ArrowRight')show(current+1);if(e.key==='ArrowLeft')show(current-1)})}document.querySelectorAll('details').forEach(detail=>detail.addEventListener('toggle',()=>{if(detail.open)document.querySelectorAll('details[open]').forEach(other=>{if(other!==detail)other.open=false})}));const form=document.querySelector('[data-contact-form]');if(form){const offer=new URLSearchParams(location.search).get('offre'),map={essentiel:'Rugby',club:'Club / Saison',marque:'Marque / Média'};if(map[offer])form.elements.project.value=map[offer];form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const data=new FormData(form),subject='Demande photo — '+data.get('project'),body=['Nom : '+data.get('name'),'E-mail : '+data.get('email'),'Téléphone : '+(data.get('phone')||'Non renseigné'),'Projet : '+data.get('project'),'Date / période : '+(data.get('date')||'À définir'),'',data.get('message')].join('\n');form.querySelector('.form-status').textContent='Votre demande est prête dans votre application e-mail.';location.href='mailto:axel.hicauber.photography@gmail.com?subject='+encodeURIComponent(subject)+'&body='+encodeURIComponent(body)})}})();